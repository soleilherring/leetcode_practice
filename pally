class Solution:
    def isPalindrome(self, x: int) -> bool:
        if x < 0:
           return False

        num = 0
        copy_x = x
        while copy_x > 0:
            remainder = copy_x %10 
            num = num * 10 + remainder
            copy_x //=10
        return x == num
